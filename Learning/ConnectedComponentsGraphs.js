// For the same example, we will create an array of size 11 (n+1) starting with the zeroth index. Initialize this visited array to zero, indicating that all the nodes are unvisited. Then follow the following algorithm. 
// If a node is not visited, then call the traversal algorithm.


 

//  for(i = 1; i <= 10; i++){
//       if(!visited){
//            traversal(i)
//       }
//  }